# Collaboration Guidelines

- Use English for logs. User-facing prompts may be written in Chinese.
- Write commit messages in English using the format `type(scope): short description`.
- Allowed commit types: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`.
