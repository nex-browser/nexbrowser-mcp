# Example values for a trusted shell or MCP-client environment. The CLI does not auto-load .env.
# Never commit a real OpenAPI token.
NEX_API_HOST=http://127.0.0.1:45536
NEX_API_KEY=
NEX_TIMEOUT=30000
# Optional: expose sensitive raw CDP endpoints in management results.
NEX_EXPOSE_CDP=0
